# Database Connection (PostgreSQL for production via Neon / Supabase, or SQLite for zero-config local dev)
# Hosted PostgreSQL (Neon / Supabase):
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.xxxx.supabase.co:5432/postgres"

# NextAuth Configuration
NEXTAUTH_SECRET="loop-enterprise-ai-feedback-super-secret-key-32chars"
NEXTAUTH_URL="http://localhost:3000"

# Anthropic Claude API (Server-side only — never exposed to client)
ANTHROPIC_API_KEY="sk-ant-api03-..."
ANTHROPIC_MODEL="claude-sonnet-4-6"
